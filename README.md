Technologies

	Database – Using h2 console as a simple sql database for the application. Allowed for easy backend persistence.
	 Mockito – To fake any query’s such that the tests still pass without an active database 
	AngularJS – Allows for dynamic updating for the accounts page. This was implemented in the delete section of the application, however it does not work for editing an account.

